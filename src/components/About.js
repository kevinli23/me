import React from 'react';
import { makeStyles, Typography } from '@material-ui/core';

const About = () => {
    return(
        <div className={"about"}>
        </div>
    );
}

export default About;